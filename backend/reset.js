require('dotenv').config(); const pool = require('./db'); const bcrypt = require('bcryptjs'); async function reset() { try { const salt = await bcrypt.genSalt(10); const hash = await bcrypt.hash('AmberFlow123!', salt); await pool.query('UPDATE users SET password = $1 WHERE email = $2', [hash, 'niktesla345@gmail.com']); await pool.query('UPDATE users SET password = $1 WHERE email = $2', [hash, 'admin@amberflow.in']); console.log('Successfully reset passwords.'); process.exit(); } catch (e) { console.error(e); process.exit(1); } } reset();
