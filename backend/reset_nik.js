require('dotenv').config(); const pool = require('./db'); const bcrypt = require('bcryptjs'); async function reset() { try { const salt = await bcrypt.genSalt(10); const hash = await bcrypt.hash('AmberFlow123!', salt); await pool.query('UPDATE users SET password = $1 WHERE email = $2', [hash, 'test_customer1@example.com']); console.log('Successfully reset NIK TESLA password.'); process.exit(); } catch (e) { console.error(e); process.exit(1); } } reset();
